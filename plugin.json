{
  "name": "ctx",
  "version": "@version@",
  "description": "Keeps Claude Code's context small: routes bulk reads and boilerplate to a cheap non-Claude worker (Gemini via agy, sandboxed), blocks commands that would dump big files into context, and past a context threshold asks for an ekko handoff before the turn ends.",
  "homepage": "https://github.com/thiagoproldan/ctx",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Read",
        "hooks": [
          { "type": "command", "command": "@root@/hooks/check-file-size" }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          { "type": "command", "command": "@root@/hooks/check-bash-read" }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Bash|Read",
        "hooks": [{ "type": "command", "command": "@root@/hooks/track-usage" }]
      }
    ],
    "SessionStart": [
      {
        "hooks": [
          { "type": "command", "command": "@root@/hooks/announce-graph" }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [{ "type": "command", "command": "@root@/hooks/handoff" }]
      }
    ]
  }
}
